(function(window) {
    window["env"] = window["env"] || {};

    // Environment variables
    window["env"]["countryServiceUrl"] = "${COUNTRY_SERVICE_URL}";
    window["env"]["currencySerivceUrl"] = "${CURRENCY_SERVICE_URL}";
    window["env"]["lanugageServiceUrl"] = "${LANGUAGE_SERVICE_URL}";
})(this);